<nav class="recipe-page-tabs" aria-label="Navigation des recettes">
    <button type="button" class="recipe-page-tab{{ activeTab == 'community' ? ' is-active' : '' }}" data-recipe-tab="community" aria-selected="{{ activeTab == 'community' ? 'true' : 'false' }}">Communauté</button>
    <button type="button" class="recipe-page-tab{{ activeTab == 'mine' ? ' is-active' : '' }}" data-recipe-tab="mine" aria-selected="{{ activeTab == 'mine' ? 'true' : 'false' }}">Mes recettes</button>
</nav>
