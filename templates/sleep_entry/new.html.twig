{% extends 'base.html.twig' %}

{% block body %}
    <div class="form-page">
        <div class="form-card">
            <header class="form-header">
                <h1>Ajouter une nuit de sommeil</h1>
            </header>

            {{ form_start(form) }}

            <div class="form-grid">
                {{ form_row(form.date) }}
                {{ form_row(form.bedTime) }}
                {{ form_row(form.wakeUpTime) }}
                {{ form_row(form.quality) }}
                {{ form_row(form.note) }}
            </div>

            <div class="form-actions">
                <button type="submit" class="form-button form-button-primary">
                    Ajouter la nuit
                </button>
            </div>

            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}